<div class="flex flex-col h-full items-center justify-center">
  <div class="flex flex-row space-x-4 mb-3">
    <% previous_url = request.referer %>
    <% edit_path = edit_image_core_path %>
    <%= link_to "Edit", edit_image_core_path(@image_core), class: "edit-button" %>
    <% if previous_url && !previous_url.include?(edit_path) %>
      <%= link_to "Back", "javascript:history.back()", class: "back-button" %>
    <% else %>
      <%= link_to "Back to Index", root_path, class: "back-button" %>
    <% end %>
    <%= button_to "Destroy", @image_core, method: :delete,  data: { confirm: 'Are you sure you want to delete this upload?' }, class: "delete-button" %>
  </div>
  <%= render partial: "image_core", locals: {image_core: @image_core} %>
</div>
